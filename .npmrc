# pnpm settings — ensure a single copy of React / React Router / Hydrogen across the
# workspace so the symlinked library and the example app never load two React instances.
dedupe-peer-dependents=true
public-hoist-pattern[]=*react*
public-hoist-pattern[]=react-router*
public-hoist-pattern[]=@shopify/hydrogen
