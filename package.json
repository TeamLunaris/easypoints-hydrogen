{
  "name": "easypoints-hydrogen-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "vp run -r build",
    "dev": "vp run -r dev",
    "check": "vp run -r check",
    "test": "vp run -r test"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "vite-plus": "^0.1.24"
  }
}
