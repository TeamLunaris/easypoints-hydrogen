// Public type surface for `@lunaris/easypoints-hydrogen/types`.
//
// Placeholder only — the real easyPoints domain types (CustomerLoyaltyMetafield,
// ShopLoyaltyValue, PointRule, TierRule, Tier, CustomerTierMaintenance, ErrorResponse,
// ApiResponse, CreateCoupon*, LoyaltyEnv, and the camel/snake case helpers) are ported in
// the follow-up phase from solaris-cards-storefront (app/lib/easy-points/types.d.ts).
//
// TODO(follow-up plan): replace this with the ported domain types.
export type Placeholder = unknown;
